# Copy to deploy.env (untracked). Account-specific values for scripts/deploy.sh.
PLUGIN_IDS="123456 234567"        # the private plugin ids to update (settings page URL)
NODE="google_calendar_123456"     # the native calendar instance's merge-variable node
